# 마음씨앗 우체국 접근성 점검

점검일: 2026-08-24  
목표: WCAG 2.2 AA 핵심 원칙과 UDL(이해·표현·참여의 다양한 방식)을 Demo Mode에 반영

## 1. 이미 잘 구현된 접근성

- 학생 핵심 메뉴는 큰 Emoji/그림과 짧은 한글을 함께 사용한다.
- 주요 학생 카드는 넓은 터치 영역이며 터치·마우스·스타일러스에 Pointer Events를 사용한다.
- 오늘 내 마음은 Emoji만으로도 완료할 수 있고 글·그림·녹음은 선택 사항이다.
- 오늘의 발견은 사진 또는 빈 종이 그림 중 하나를 선택할 수 있으며 녹음은 필수가 아니다.
- 공개범위는 `👀 함께 볼래요`, `🧑‍🏫 선생님과 나만`, `🔒 나만 볼래요`로 아이콘과 문자를 함께 사용하며 자동 선택하지 않는다.
- 드로잉에는 지우개, 되돌리기, 전체 지우기 확인, 펜 굵기, 큰 색상 버튼이 있다.
- 스티커는 선택 후 큰 삭제·크기 조절 버튼을 제공한다.
- 학생 종료에는 확인 단계가 있고 공용기기에서는 이전 학생 상태를 제거한다.
- 상태는 색만이 아니라 `✓ 했어요`, `○ 아직`, `켜짐/꺼짐` 문자를 함께 사용한다.
- 마이크 오류는 기술 오류 대신 소리 없이 계속할 수 있다는 쉬운 안내를 제공한다.
- 학생/교사/device 경로와 권한 판정이 분리되어 접근성 지원이 privacy rule을 변경하지 않는다.

## 2. 발견한 문제

- 키보드 focus 표시가 화면 전체에서 충분히 강하지 않았다.
- 스티커 이동이 사실상 drag 중심이어서 미세운동·스위치 사용자가 조작하기 어려웠다.
- Emoji 버튼의 accessible name이 기호 자체에 의존했다.
- 읽기 전 단계 학생이 핵심 안내를 선택해서 들을 공통 기능이 부족했다.
- reduced-motion 기기 설정을 명시적으로 존중하는 CSS가 없었다.
- 자동 종료 시간이 모든 학생에게 동일했다.
- 접근성 선호를 학급 및 학생 단위로 설정할 데이터/UI가 없었다.
- 학생별 선택지 수를 줄이는 기능이 실제 Emoji·스티커 목록과 연결되지 않았다.
- 일부 로딩·오류 상태에 screen reader용 live status가 부족했다.
- 사진의 일부 `alt`가 비어 있었다.

## 3. 수정한 문제

- 전역 `:focus-visible`에 고대비 녹색 outline과 흰색 외곽 여백을 적용했다.
- 스티커를 선택한 뒤 `← ↑ ↓ →` 버튼과 키보드 Arrow keys로 옮기는 대안을 추가했다. drag, 크기 조절, 삭제 기능은 유지했다.
- 감정·자연 Emoji 버튼에 `aria-label`과 `aria-pressed`를 추가했다.
- 오늘 내 마음, 사진/자연 표현, 오늘의 발견, 공개범위, 종료 안내에 선택형 `🔊 들어볼래요`를 추가했다. 자동재생하지 않는다.
- `prefers-reduced-motion: reduce`와 교사 설정의 움직임 감소를 반영했다.
- 학급 기본 및 학생별 기능적 선호(`largeTargets`, `audioPrompts`, `simplifiedChoices`, `pictureGuidance`, `reducedMotion`, `extendedTimeout`, `tapAlternativeToDrag`, `teacherHelp`)를 저장한다. 진단명은 저장하지 않는다.
- 시간 연장 설정 시 inactivity 경고와 추가 시간을 3배로 늘린다.
- 선택지 단순화 시 마음 Emoji·자연 상징·스티커 수를 줄인다.
- 접근성 설정 switch에 `role=switch`, `aria-checked`, 켜짐/꺼짐 문자를 제공했다.
- 로딩에는 `role=status`, 학생 오류에는 `role=alert`, 종료/시간 확인에는 dialog semantics를 추가했다.
- 의미 있는 학생·교사 작품 사진에 일반적인 대체정보를 추가했다. 사진 내용을 AI로 추정하지 않는다.
- coarse pointer 환경의 핵심 학생 버튼은 최소 56px 터치영역을 확보하도록 보완했다.

## 4. 아직 남아 있는 문제

- Web Speech API 음성 품질과 한국어 음성 제공 여부는 OS/브라우저에 따라 달라진다. 운영 버전은 녹음된 시스템 안내 또는 검증된 TTS 자산을 고려해야 한다.
- 브라우저 기본 `confirm()`은 기본 focus trap을 제공하지만 유아 친화적인 큰 버튼 dialog로 통일되지 않았다.
- Canvas 드로잉 자체를 screen reader로 완전히 표현하는 것은 어렵다. 그림 외 Emoji·사진·선택 방식이 대체 표현수단이다.
- 학생 사진의 구체적 대체설명은 자동 생성하지 않는다. 향후 교사가 선택적으로 설명을 추가하는 필드가 필요하다.
- 녹음 중 경과시간 표시는 아직 없으며 현재는 `녹음 중/끝내기` 버튼 상태로만 알린다.
- 실제 switch scanning engine, eye-gaze 전용 연동, AAC 기기 직접 연동은 MVP 범위 밖이다. 표준 키보드 이벤트와 focus navigation을 기반으로 준비했다.
- localStorage Demo이므로 접근성 선호의 다기기 동기화는 Supabase 연결 후 필요하다.

## 5. 실제 기기에서 추가 확인할 항목

- iOS VoiceOver, Android TalkBack, Windows NVDA, macOS VoiceOver에서 읽기 순서와 accessible name.
- Tab/Shift+Tab/Enter/Space 및 switch-compatible 입력으로 학생 입장부터 저장·종료까지 수행.
- iPad/Android 태블릿/전자칠판에서 56px touch target과 스티커 화살표 조작.
- 브라우저 200%/400% 확대와 360px 폭에서 reflow, 가로 overflow, sticky/focus 가림.
- 가로·세로 회전과 화면 키보드가 열린 상태의 입력필드 가시성.
- 실제 카메라·마이크 권한 거부 후 그림/Emoji만으로 활동 지속.
- 반응 시간이 긴 학생의 연장된 inactivity 경고 및 복귀 흐름.
- 고대비 모드와 색각 차이 환경에서 선택·완료·공개범위 상태 식별.

## 6. WCAG 관련 주요 확인사항

- 1.1.1 대체 텍스트: UI 아이콘은 텍스트와 병기하고 의미 있는 사진에는 일반 alt를 제공한다. 학생 사진 내용을 추정하지 않는다.
- 1.3.1/1.3.2 구조와 순서: 표준 button, label, input, heading, details를 사용하며 DOM 순서와 시각 순서를 일치시킨다.
- 1.4.1 색 사용: 완료·선택·ON/OFF를 색과 문자/아이콘으로 함께 표현한다.
- 1.4.3 대비: 본문과 주요 버튼은 진한 녹색/짙은 본문색을 유지한다. 실제 대비 수치는 디자인 토큰 변경 시 재측정한다.
- 1.4.10 Reflow: auto-fit grid와 모바일 1~2열 레이아웃을 사용한다. 200% 실제 기기 확인이 남아 있다.
- 2.1.1 키보드: 표준 버튼과 입력을 사용하고 스티커 Arrow key 조작을 추가했다.
- 2.2.1 시간 조절: 개인정보 보호 자동 종료 전 경고를 제공하고 학생별 시간 연장을 지원한다.
- 2.3.3 움직임: prefers-reduced-motion과 개별 설정을 존중한다. 깜박임 UI는 없다.
- 2.4.7/2.4.11 Focus: 명확한 focus-visible outline을 제공한다.
- 2.5.5 Target Size: 학생 핵심 조작은 56px 이상을 목표로 한다.
- 3.3 오류: 기술 코드 대신 쉬운 복구 안내를 제공하고 입력 label을 유지한다.
- 4.1.2 Name/Role/Value: Emoji 선택, 접근성 switch, 상태 영역에 name/role/state를 보완했다.

## 7. 터치/키보드/스크린리더 확인사항

- 터치: 학생 메뉴, Emoji, 공개범위, 녹음, 저장, 종료, 스티커 조작은 큰 버튼으로 제공한다.
- 키보드: focus-visible을 유지하고 모든 핵심 메뉴는 native button이다. 스티커는 Arrow keys를 지원한다.
- 스크린리더: Emoji에는 의미 이름과 선택 상태가 있고 로딩/오류는 status/alert로 알린다. 장식 Emoji의 추가 `aria-hidden` 정리는 계속 점검한다.
- Drag 대안: 스티커는 끌기와 누르기 화살표를 함께 제공한다. multi-touch는 필수가 아니다.
- 청각 대안: 시스템 안내는 항상 화면 글/그림과 함께 제공되며 음성만으로 전달하지 않는다.
- 말하기 대안: Emoji, 그림, 사진, 스티커만으로도 참여 가능하며 녹음은 선택이다.

## 8. 중증장애학생 접근 시 추가 검토사항

- 읽기 어려움: 큰 아이콘+짧은 글, 선택형 음성 안내, 그림 중심 안내를 사용한다.
- 말하기 어려움: Emoji와 상징 선택만으로 오늘 마음을 저장할 수 있다.
- drag 어려움: 스티커 선택 후 화살표 버튼/키보드로 이동한다.
- 느린 반응: 학생별 자동 종료 시간 연장을 켤 수 있다.
- 선택 과부하: 학생별 선택지 단순화를 켜 Emoji와 스티커 수를 줄인다.
- 교사 신체 지원: 교사와 함께하기에서 학생의 의사를 확인한 뒤 교사가 눌러줄 수 있으며 공개범위 규칙은 바뀌지 않는다.
- switch/키보드: 표준 focus와 Enter/Space, Arrow key 조작을 사용한다. 실제 보조기기 검증이 필요하다.

## 화면별 점검표

| 화면 | 적용/확인 내용 |
|---|---|
| 서비스 첫 화면·학생/선생님 선택 | 큰 아이콘+글, 두 개의 명확한 표준 버튼, 모바일 reflow |
| 학생 QR/코드 입장 | 학생 명단 미노출, label이 있는 입력, 쉬운 오류, 큰 진입 버튼 |
| 교사 로그인 | label 기반 입력, 키보드 접근, 신규 학급 흐름 유지 |
| 학생 홈 | ON 기능만 표시, 큰 아이콘+글, 큰 종료 버튼, 접근성 선호 적용 |
| 오늘 내 마음 | Emoji accessible name/selected state, 선택형 음성, 글/녹음 선택 |
| 공개범위 | 자동 선택 없음, 아이콘+글, 선택형 음성, privacy 유지 |
| 마음친구 | 글·그림·음성 선택, privacy가 허용하지 않은 친구 내용 비노출 |
| 자연마음·사진 | 카메라 외 그림/꾸미기/소리 방식 유지, 권한 오류 시 계속 가능 |
| 그림판·스티커 | 흰 기본 배경, 큰 펜색, Undo, drag+tap+keyboard 이동, 삭제 버튼 |
| 내 보물 찰칵·오늘의 발견 | 사진/그림/스티커/음성 조합, 교사 음성 안내 재생 |
| 마음정원·보물창고 | 개인 세션에서만 열림, 비교/순위 없음 |
| 마음편지 | Emoji·사진·그림·글·음성, 학생 본인 편지만 열람 |
| 학생 종료 | 확인 dialog 의미, 상태 제거 후 안전 화면 복귀 |
| 교사와 함께하기·대집단 | 교사 카드 선택 지원, 완료 여부만 표시, privacy 유지 |
| 교사 대시보드 | hover 비의존 카드 버튼, 키보드 focus, 반응형 grid |
| 감정 분석·관계지도 | self_only 제외, 색만으로 상태 표현하지 않음, 순위화 없음 |
| 갤러리·인쇄 | 승인/class_share만 공용 표시, 배경색과 대체정보 유지 |
| 학생·QR 관리 | label/표준 입력, 보안 확인, 키보드 조작 |
| 기능 설정 | OFF 기능은 학생 홈에서 숨김, ON/OFF 문자 병기 |
| 접근성 지원 | 진단명 없는 기능 선호, 학급 기본+학생별 override, switch semantics |
